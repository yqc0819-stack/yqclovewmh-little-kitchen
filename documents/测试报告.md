# 我们的小厨房测试报告

测试日期：2026-07-03

## 本次新增

- 菜品管理窗口支持修改菜品。
- 可修改名称、分类、说明和备用表情。
- 可上传新照片替换原照片。
- 可移除照片并恢复使用表情。
- 编辑保持原菜品 ID，因此今日菜单和历史记录不会断开。
- 删除菜品时同步清理所有关联。

## 自动检查结果

### 基础功能装配：39/39 通过

- 菜品库：新增、管理、修改、删除。
- 图片：选择、压缩、预览、替换、移除。
- 发布：选择今日菜品、发布今日菜单。
- 点餐：选择菜品、口味备注、快捷备注、提交。
- 提醒：页面红点、选择结果展示。
- 状态：已看到、准备中、已完成。
- 历史：点餐端和管理端历史记录。
- 持久化：浏览器本地读取和保存。
- 云端：登录、数据加载、菜单发布、点餐提交与实时刷新入口。
- 页面：点餐端与管理端切换。
- 结构：无重复 HTML ID、CSS 括号完整、脚本资源齐全。

### 菜品 CRUD 逻辑：9/9 通过

- 修改名称成功。
- 替换图片成功。
- 移除图片成功。
- 编辑后菜品 ID 不变。
- 编辑后今日菜单关联保留。
- 编辑后历史关联保留。
- 编辑不会意外修改原测试数据。
- 删除菜品成功。
- 删除后所有关联清理成功。

## 浏览器限制

当前页面通过 `file://` 打开。内置浏览器安全策略禁止自动化控制本地文件，因此无法执行自动鼠标点击测试。代码联网部署到 HTTPS 地址后，应再执行一次两台手机的真实端到端测试。

## 重复运行检查

在 PowerShell 中运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "& ([ScriptBlock]::Create((Get-Content -Raw -Encoding UTF8 '.\website\tests\regression-check.ps1')))"
```
